import{fmt,totals}from'./document.js';
export function renderPreview(el,items,showCalc){const t=totals(items);const rows=items.map((i,idx)=>`<tr><td>${idx+1}</td><td>${esc(short(i.productName,46))}</td><td class="num">${fmt(i.pallets)}</td><td class="num">${fmt(i.boxes)}</td>${showCalc?`<td>${esc(i.calc)}</td>`:''}<td class="num">${fmt(i.total)} ${esc(i.unit)}</td></tr>`).join('');const unitRows=Object.entries(t.byUnit).map(([u,v])=>`<div>${esc(u)}: <b>${fmt(v)}</b></div>`).join('');el.innerHTML=`<div class="paper"><h2>Облік готової продукції</h2><div class="paper-meta"><span>Дата: ${new Date().toLocaleDateString('uk-UA')}</span><span>Час: ${new Date().toLocaleTimeString('uk-UA',{hour:'2-digit',minute:'2-digit'})}</span></div><table><thead><tr><th>№</th><th>Продукція</th><th>Підд.</th><th>Ящ.</th>${showCalc?'<th>Розрахунок</th>':''}<th>Разом</th></tr></thead><tbody>${rows}</tbody></table><div class="paper-summary"><div>Усього піддонів: <b>${fmt(t.pallets)}</b></div><div>Усього ящиків: <b>${fmt(t.boxes)}</b></div>${unitRows}</div></div>`}
function esc(s){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
function short(s,n){return s.length>n?s.slice(0,n-1)+'…':s}
